# Screenshots

Drop the six images the main README links to in this folder, using these exact names.

| File | What to capture |
|---|---|
| `canvas.jpeg` | The main Canvas screen with a drawing on it and the toolbar sheet pulled up |
| `configure.jpeg` | The Configure tools sheet, with several chips switched off |
| `examples.jpeg` | The Examples gallery, scrolled so three or four cards are visible |
| `signature.jpeg` | Signature Pad, signed, showing the delivery form around it |
| `markup.jpeg` | Photo Markup with a photo loaded and a circle plus a text label on it |
| `neon.jpeg` | Neon Art, a few glowing strokes, showing the dark toolbar |

Capture on a phone or emulator in portrait. In Android Studio use Running Devices, or from a
terminal:

```bash
adb exec-out screencap -p > docs/images/canvas.png   # then convert, or screenshot on device
```
